# 03-核查表

> 核查时间：2026-07-14
> 核查范围：25 篇笔记，每篇 5 条要点，共 125 条
> 核查方法：逐条回原文比对，数字/结论/技术细节/引用路径重点核查

---

## 最近 OpenClaw 这么火，你知道它的原理吗？

| 笔记条目 | 状态 | 正文依据 |
|---------|------|---------|
| 要点1：OpenClaw 定位为"多渠道 AI 网关"，自身不做推理，只做调度和执行 | 已核实 | 13216 回答重点第1段："OpenClaw 本质上是一个开源的多渠道 AI 网关，它自己不做推理，只做调度和执行" |
| 要点2：核心运行时 Agentic Loop（ReAct 范式）六步循环 | 已核实 | 13216 回答重点第6-9段："Agentic Loop…基于 ReAct 范式的推理循环…Load→Call→Parse→Execute→Append→Loop" |
| 要点3：Gateway 是核心独有模块，7×24 运行，承担六件事 | 已核实 | 13216 扩展知识-Gateway 网关第1段："Gateway 才是 OpenClaw 区别于其他 AI 编程工具的核心独有模块"，后续六节分别对应六项职责 |
| 要点4：Skills 采用渐进式披露，元数据层轻量，完整指令命中时加载 | 已核实 | 13216 扩展知识-技能系统第1-4段描述按需加载机制，包含三层加载逻辑 |
| 要点5：记忆系统混合搜索（BM25+向量），时间衰减、MMR 重排，数据本地 | 已核实 | 13216 扩展知识-记忆系统第1-6段："混合搜索策略…BM25 关键词匹配加向量余弦相似度…时间衰减…MMR 多样性重排…数据全部存在本地" |

---

## （OpenClaw前置知识）什么是 AI Agent？它和直接调用大模型 API 做一次问答有什么本质区别？

| 笔记条目 | 状态 | 正文依据 |
|---------|------|---------|
| 要点1：本质区别三点：工具调用、记忆和上下文、自主决策循环 | 已核实 | 13217 回答重点第2-4段："本质区别有三点：1）Agent 有工具调用能力…2）Agent 有记忆和上下文…3）Agent 有自主决策循环" |
| 要点2：OpenClaw 内置 25 个核心工具 | 已核实 | 13217 扩展知识-Agent 的核心架构第2段："OpenClaw 内置了文件读写、Shell 执行、浏览器控制、Web 搜索、记忆检索等 25 个核心工具" |
| 要点3：OpenClaw 一次复杂任务 API 费用 2-5 美元，10 轮 3-15 万 token | 已核实 | 13217 扩展知识-Agent 的常见坑点第1段："OpenClaw 一次复杂任务跑下来，光 API 费用可能就 2-5 美元。10 轮循环下来可能吃掉 3-15 万 token" |
| 要点4：RAG 是 Agent 的工具，Agent 解决复杂任务执行 | 已核实 | 13217 面试官追问第2问："RAG 本质上可以看作 Agent 的一个工具。RAG 解决的是'让模型获取外部知识'的问题，Agent 解决的是'让模型执行复杂任务'的问题" |
| 要点5：token 优化四方向：上下文压缩、工具结果精简、分层调度、缓存 | 已核实 | 13217 面试官追问第4问："1）上下文压缩…2）工具结果精简…3）分层调度…4）缓存" |

---

## （OpenClaw前置知识）请解释 Tool Calling（工具调用）的完整链路

| 笔记条目 | 状态 | 正文依据 |
|---------|------|---------|
| 要点1：核心链路四步：定义工具→LLM 决策→系统执行→结果回传 | 已核实 | 13221 回答重点第1-7段："Tool Calling 的核心链路就四步：定义工具 → LLM 决策 → 系统执行 → 结果回传" |
| 要点2：2023 年 11 月起支持 parallel tool calls | 已核实 | 13221 扩展知识第1段："2023 年 11 月，升级成了 Tool Calling，最大的变化是支持 parallel tool calls" |
| 要点3：工具超过 15-20 个时 LLM 选择准确率明显下降 | 已核实 | 13221 面试官追问第4问："工具数量不能太多，超过 15-20 个的时候 LLM 选择准确率会明显下降" |
| 要点4：各 Provider Schema 差异 | 已核实 | 13221 扩展知识第3段："Gemini 不支持 patternProperties 和 additionalProperties…xAI 不支持 minLength、maxLength 约束，OpenAI 要求参数顶层必须是 type: \"object\"" |
| 要点5：安全三层：工具白名单、参数校验、敏感操作人工确认 | 已核实 | 13221 扩展知识-安全和权限控制第1-2段："1）工具白名单…2）参数校验…3）敏感操作加人工确认" |

---

## （OpenClaw前置知识）System Prompt 在 Agent 系统中承载了哪些职责？

| 笔记条目 | 状态 | 正文依据 |
|---------|------|---------|
| 要点1：五大职责：角色定义、工具规范、输出格式、安全权限、上下文补充 | 已核实 | 13222 回答重点第3-7段："1）角色定义和行为准则…2）工具使用规范和约束…3）输出格式要求…4）安全与权限控制…5）上下文信息补充" |
| 要点2：处理策略三字：拆、选、扔 | 已核实 | 13222 回答重点第9段："核心策略就三个字：拆、选、扔" |
| 要点3：模块顺序影响行为，OpenClaw 拼接顺序 Identity→Tooling→Safety 最前 | 已核实 | 13222 面试官追问第2问："OpenClaw 的模块拼接顺序是固定写在 buildAgentSystemPrompt 函数里的：Identity → Tooling → Safety 在最前面" |
| 要点4：超过 500 token 且使用频率低于 30% 放外部 | 已核实 | 13222 面试官追问第3问："超过 500 token 且使用频率低于 30% 的信息，放外部就更经济" |
| 要点5：分层防御：System Prompt 红线+代码级硬控制 | 已核实 | 13222 面试官追问第1问："System Prompt 里有 Safety 模块写明安全红线…真正的保障在工具执行层…before_tool_call 钩子…exec 工具有审批机制" |

---

## （OpenClaw前置知识）什么是 Agent 的 Context Window？

| 笔记条目 | 状态 | 正文依据 |
|---------|------|---------|
| 要点1：token 构成量化：System Prompt 2000-5000，工具定义 4000-10000，对话历史最易膨胀 | 已核实 | 13223 扩展知识第1-5段："System Prompt 通常就有 2000-5000 token…注册 20 个工具就是 4000-10000 token…一条消息就可能占掉 3000-8000 token。跑 10 轮下来，对话历史轻松突破 50K token" |
| 要点2：硬下限 16,000 token，软告警 32,000 token | 已核实 | 13223 扩展知识-OpenClaw 的 Context Window 管理机制第2-3段："CONTEXT_WINDOW_HARD_MIN_TOKENS = 16,000…CONTEXT_WINDOW_WARN_BELOW_TOKENS = 32,000" |
| 要点3：中文 token 开销比英文高 2-3 倍，不同模型 tokenizer 差 10%-20% | 已核实 | 13223 扩展知识-token 计算的坑第1-3段："同样一段中文内容，token 开销比等长的英文内容高 2-3 倍…同一段文本在 GPT 和 Claude 里算出来的 token 数可能差 10%-20%" |
| 要点4：OpenClaw 用各模型对应 tokenizer 精确计算，留 10% 安全余量 | 已核实 | 13223 扩展知识-token 计算的坑第4段："用各模型对应的 tokenizer 做精确计算，同时留出 10% 的安全余量" |
| 要点5：四种主流策略：滑动窗口、摘要压缩、分层存储、外部检索 | 已核实 | 13223 扩展知识-主流的 Context 管理策略："1）滑动窗口…2）摘要压缩…3）分层存储…4）外部检索…实际生产中一般是混合使用" |

---

## （OpenClaw前置知识）解释「短期记忆」和「长期记忆」在 Agent 系统中的区别

| 笔记条目 | 状态 | 正文依据 |
|---------|------|---------|
| 要点1：短期记忆精度高容量有限，长期记忆容量无限但检索有损 | 已核实 | 13224 回答重点第3-5段："短期记忆精度高但容量有限，受 context window 上限约束…长期记忆容量几乎无限，但检索有损" |
| 要点2：compaction 三种策略：滑动窗口、摘要压缩、选择性保留 | 已核实 | 13224 扩展知识-短期记忆的管理策略："1）滑动窗口…2）摘要压缩…3）选择性保留…生产环境一般是组合使用" |
| 要点3：混合检索原因：纯向量语意模糊，纯全文无法捕捉同义表达，两者互补 | 已核实 | 13224 扩展知识-长期记忆的存储和检索第3-5段："之所以需要混合是因为单纯靠向量搜索不靠谱…反之，只用全文搜索也不行…所以两者互补才是最优解" |
| 要点4：Memory Flush 是关键设计：接近压缩阈值先提取关键信息写磁盘再压缩 | 已核实 | 13224 扩展知识-短期记忆到长期记忆的自动转化第1段："Memory Flush（记忆刷盘）：当对话接近压缩阈值时，系统先触发一次 LLM 调用，让模型把关键信息提取出来写到持久化存储里，然后再做压缩" |
| 要点5：30 天未命中降权，换 embedding 模型后旧向量作废需存原文 | 已核实 | 13224 扩展知识-常见的坑第1-2段："30 天没被检索命中的记忆自动降权…embedding 模型换了之后旧的向量全部作废…要在记忆数据里同时存原文" |

---

## OpenClaw 是什么？它要解决什么问题？它的核心能力有哪些？

| 笔记条目 | 状态 | 正文依据 |
|---------|------|---------|
| 要点1：弥补两个缺口：ChatGPT 不能动手干活，Claude Code 不能 7×24 常驻 | 已核实 | 13225 回答重点第1-3段："ChatGPT / 豆包这类对话产品…不能真正'动手干活'…Claude Code…不能 7×24 小时常驻运行…OpenClaw 解决的就是这两个缺口" |
| 要点2：五大核心能力：Gateway 常驻网关、多渠道接入（15+）、模型无关引擎、插件化、本地优先 | 已核实 | 13225 回答重点第4-8段："1）Gateway 常驻网关…2）多渠道统一接入…15+ 渠道…3）模型无关的 Agent 执行引擎…4）插件化扩展体系…5）本地优先 + 隐私安全" |
| 要点3：Gateway 跑在用户自己机器上，默认绑定 127.0.0.1:18789，不对外暴露 | 可疑 | **"127.0.0.1:18789" 这一具体端口号在 13225 原文中未出现**，该细节出自 13216（第一篇）扩展知识-Gateway 网关第1段。13225 中仅提到"Gateway 跑在你自己的机器上"和"数据完全在本地"，未提及端口绑定地址。笔记将两篇原文的信息合并到了同一个要点中，核心语义（本地运行、不经过第三方）正确，但端口细节的出处标注有误 |
| 要点4：多设备会话同步天然实现，所有渠道消息汇聚到同一 Gateway | 已核实 | 13225 面试官追问第1问："会话数据存在 Gateway 所在的机器上，手机和电脑连的是同一个 Gateway 实例，所以天然就是同步的" |
| 要点5：默认不支持动态切换 Agent，路由按渠道/群组静态绑定，动态切换需插件 | 已核实 | 13225 面试官追问第2问："路由规则是预先配好的，按渠道或群组匹配，不支持用户在对话中动态切换…写个插件在 before_agent_start Hook 里做动态路由" |

---

## OpenClaw 的核心组件有哪些？请描述它们之间的关系

| 笔记条目 | 状态 | 正文依据 |
|---------|------|---------|
| 要点1：五层组件：Channel Plugins→Gateway→Routing→Agent Runner→Context Engine | 已核实 | 13226 回答重点第1-5段："Channel Plugins 是最外层…Gateway 是整个系统的中枢…Routing 是路由层…Agent Runner 是执行引擎…Context Engine 管对话历史" |
| 要点2：分层原因：Channel 屏蔽协议差异，Gateway 收敛横切，Routing 多 Agent 调度 | 已核实 | 13226 扩展知识-为什么要分这么多层："如果不抽出 Channel 层做协议转换…Gateway 单独拎出来也是同理…Routing 独立出来是因为 Agent 系统往往不止一个 Agent" |
| 要点3：Context Engine 接口在 types.ts，核心三件事：ingest/assemble/compact | 已核实 | 13226 扩展知识-源码层面的落点第5段 + 面试官追问第2问："Context Engine 在 src/context-engine/types.ts 定义接口…ingest 是 no-op…assemble 直接透传…compact 委托给 compaction 模块" |
| 要点4：防死循环：超时 + tool-loop-detection.ts 四种策略 + context overflow 降级 | 已核实 | 13226 面试官追问第3问："防死循环靠多层保护：执行超时时间、工具循环检测（generic_repeat/ping_pong/known_poll_no_progress/global_circuit_breaker）、以及 context overflow 时的自动 compaction 和降级" |
| 要点5：Gateway 幂等去重通过提取消息唯一标识，本地缓存里做去重 | 已核实 | 13226 面试官追问第4问："Gateway 拿到每条消息后会提取一个唯一标识，在本地缓存里做去重判断"。补充说明：笔记中"如 Telegram message_id"的例证来自 13227（第九篇），13226 原文仅说"提取一个唯一标识"，未具体举例 |

---

## 在 OpenClaw 中，一条用户消息从进入系统到收到回复，完整链路是怎样的？

| 笔记条目 | 状态 | 正文依据 |
|---------|------|---------|
| 要点1：四阶段 8 步：渠道适配器→MsgContext→dispatchInboundMessage→路由→斜杠命令→runReplyAgent→LLM+工具循环→ReplyDispatcher | 已核实 | 13227 回答重点第1-7段："整条链路可以分成入站、路由、执行、出站四个阶段，一共 8 步"并逐步描述 |
| 要点2：两层排队：enqueueSession（session 串行）+ enqueueGlobal（全局限流） | 已核实 | 13227 扩展知识-排队机制："两层排队：enqueueSession 和 enqueueGlobal。enqueueSession 保证同一个 session 内的消息串行执行…enqueueGlobal 是全局限流" |
| 要点3：Fallback 是 model-level 的，只切换 provider 和 model | 已核实 | 13227 扩展知识-Fallback 机制 + 面试官追问第2问："fallback 只切换 provider 和 model，系统提示词、工具列表等 Agent 配置保持不变" |
| 要点4：idempotencyKey 用渠道消息 ID（Telegram message_id、Discord snowflake），非内容生成 | 已核实 | 13227 面试官追问第3问："idempotencyKey 不是根据消息内容生成的，通常是用渠道推送过来的消息 ID，比如 Telegram 的 message_id、Discord 的 message snowflake" |
| 要点5：Telegram 不支持原生流式，用 sendMessageDraft 草稿接口或 editMessageText 模拟 | 已核实 | 13227 扩展知识-流式输出第3段："Telegram 没有原生的 WebSocket 流式推送，OpenClaw 用了两种模拟策略：优先使用 Telegram Bot API 较新的草稿消息接口（sendMessageDraft），如果不可用则 fallback 到先发一条消息再用 editMessageText 循环更新" |

---

## OpenClaw 的 Agent Runner 是如何工作的？一次 Agent 运行经历了哪些阶段？

| 笔记条目 | 状态 | 正文依据 |
|---------|------|---------|
| 要点1：六阶段：排队→准备→插件+Hook→模型解析+鉴权→尝试执行→溢出降级 | 已核实 | 13228 回答重点第1-6段："1）排队…2）准备…3）插件 + Hook…4）模型解析 + 鉴权…5）尝试执行…6）溢出降级" |
| 要点2：容错两层：Auth Profile 轮转+模型级 Fallback，重试上限 32-160 次，指数退避 | 已核实 | 13228 扩展知识-attempt + fallback 容错机制："1）Auth Profile 轮转…2）模型级 Fallback…重试有上限（根据 profile 数量动态计算，范围 32-160 次）…遇到服务过载还会加指数退避" |
| 要点3：工具调用双层包装：Hook 拦截层（参数校验、权限检查、循环检测）+ 取消机制层 | 已核实 | 13228 扩展知识-工具调用的双层包装："1）Hook 拦截层…2）取消机制层：把外部的 AbortSignal 和工具自带的信号合并" |
| 要点4：Context 溢出三级降级：compaction→截断 tool result（单条最多 30%）→报错降级 | 已核实 | 13228 扩展知识-Context 溢出的三级降级："1）先尝试 compaction…2）截断超大 tool result…单个 tool result 最多占上下文窗口的 30%…3）报错降级" |
| 要点5：历史消息统一中间格式存储，fallback 切换时自动做格式适配 | 已核实 | 13228 面试官追问第2问："历史消息以统一的中间格式存储在 session 文件中…根据目标 Provider 的特性做格式适配。比如 Gemini 和 Anthropic 的 turn 交替规则不同…在 session 历史清洗阶段自动处理" |

---

## LLM 的 Context Window 有上限，长对话时如何保证 Agent 仍然能正常工作？

| 笔记条目 | 状态 | 正文依据 |
|---------|------|---------|
| 要点1：四层分层防御：Context Pruning→Tool Result Context Guard→Memory Flush→Compaction | 已核实 | 13229 回答重点第1-5段："第一层是 Context Pruning…第二层是 Tool Result Context Guard…第三层是 Memory Flush…第四层是 Compaction" |
| 要点2：Pruning 两级阈值：softTrimRatio=0.3（head+tail 裁剪，各 1500 字符），hardClearRatio=0.5（placeholder 替换），保护最近 3 条 assistant | 已核实 | 13229 扩展知识-Context Pruning 的实现细节："softTrimRatio（默认 0.3）和 hardClearRatio（默认 0.5）…保留前 1500 和后 1500 字符…keepLastAssistants=3" |
| 要点3：hasImportantTail() 检测 error/exception/exit code/traceback 关键词，优先保留尾部 | 已核实 | 13229 扩展知识-Tool Result 截断的巧妙设计："hasImportantTail() 检测尾部是否包含 error/diagnostic 内容（匹配 error、exception、exit code、traceback 等关键词）…优先保留尾部" |
| 要点4：Context Guard 预算 = context window × 4 × 0.75，从最早 tool result 开始替换 | 已核实 | 13229 扩展知识-Tool Result Context Guard 兜底机制："context budget（context window × 4 chars/token × 0.75 headroom）…从最早的 tool result 开始用 placeholder 替换" |
| 要点5：Compaction 有 identifier preservation 指令，保留 UUID、hash、URL、文件名 | 已核实 | 13229 扩展知识-Compaction 摘要的质量问题第3段："Compaction 本身还有 identifier preservation 指令，要求摘要保留 UUID、hash、URL、文件名等不可重建的标识符" |

---

## Agent 调用工具可能返回超大结果（比如代码搜索返回 50KB），这会带来什么问题？

| 笔记条目 | 状态 | 正文依据 |
|---------|------|---------|
| 要点1：50KB ≈ 12000+ token，对 128K context 吃掉近 10% | 已核实 | 13230 回答重点第2段："如果一条代码搜索返回 50KB 文本，按 1 token ≈ 4 字符估算，光这一条就吃掉 12000+ token。模型的 context window 如果是 128K token，一条 tool result 就干掉了将近 10%" |
| 要点2：两层防护：单条截断（硬上限 400K 字符，不超过 context window 30%）+ 全局预算守卫 | 已核实 | 13230 回答重点第4-5段："1）单条截断…硬上限 400K 字符…2）全局预算守卫…超过全局预算（context window 的 75%），就从最早的 tool result 开始，把内容替换成占位提示" |
| 要点3：head+tail 仅在 hasImportantTail() 检测到错误关键词时启用，tail 占 30%（上限 4000 字符），head 至少 2000 字符 | 已核实 | 13230 回答重点第3段 + 面试官追问第2问："tail 拿 budget 的 30%（上限 4000 字符），head 拿剩余的大部分空间，head 最少保留 2000 字符。只有 hasImportantTail() 检测到尾部含有 error/exception/traceback 等关键词时才分割" |
| 要点4：对 tool result 使用 TOOL_RESULT_CHARS_PER_TOKEN_ESTIMATE = 2（而非通用 4） | 已核实 | 13230 扩展知识-字符预算的计算第3段："OpenClaw 对 tool result 用了不同的 token 换算系数（2 而非 4），因为代码和结构化文本的 token 密度比自然语言高" |
| 要点5：截断后附加提示语告知模型内容被截断，建议 offset/limit 重新获取 | 已核实 | 13230 面试官追问第1问："截断后缀会明确告诉模型 Content truncated，并建议使用 offset/limit 参数或请求特定部分来获取更多内容" |

---

## 当对话历史实在太长、裁剪也不够用时，还有什么办法？什么是 Compaction？

| 笔记条目 | 状态 | 正文依据 |
|---------|------|---------|
| 要点1：四步流程：分块→逐块摘要→合并摘要→摘要增强，核心是"信息保全式压缩" | 已核实 | 13231 回答重点第1-5段："1. 分块（Chunking）…2. 逐块摘要…3. 合并摘要…4. 摘要增强" + 面试问答："核心是'信息保全式压缩'" |
| 要点2：摘要必须保留 5 个结构化章节：Decisions/Open TODOs/Constraints/Pending user asks/Exact identifiers | 已核实 | 13231 扩展知识-摘要质量检查第1段："Decisions（做过的决策）、Open TODOs（未完成的任务）、Constraints/Rules（约束条件）、Pending user asks（用户尚未被回应的请求）、Exact identifiers（需要精确保留的标识符）" |
| 要点3：strict 策略精确保留 UUID、hash、API key、URL、文件名等不可重构标识符 | 已核实 | 13231 扩展知识-标识符保留策略："OpenClaw 默认采用 strict 策略，要求摘要中精确保留所有不可重构的标识符…UUID、hash、ID、token、API key、主机名、IP、端口、URL、文件名等" |
| 要点4：触发时机按 token 数而非固定轮次 | 已核实 | 13231 面试官追问第3问："按 token 数触发…用固定轮次不靠谱，因为每轮对话的长度差异很大" |
| 要点5：单次 Compaction 约几千 token，能将数万 token 压到几千，100 轮不压缩烧几十万 token | 已核实 | 13231 面试官追问第1问："单次 Compaction 大概消耗几千 token…但它能把几万 token 的对话历史压缩到几千 token 的摘要…100 轮的对话如果不压缩，光 Context 填充就要烧掉几十万 token" |

---

## OpenClaw 把 Context 管理抽象成了可插拔的 Context Engine，为什么要做这层抽象？

| 笔记条目 | 状态 | 正文依据 |
|---------|------|---------|
| 要点1：ContextEngine 接口覆盖完整生命周期：bootstrap、ingest/ingestBatch、assemble、compact、afterTurn、子 Agent 管理、dispose | 已核实 | 13232 回答重点第4段表格："初始化|bootstrap…存消息|ingest/ingestBatch…挑消息|assemble…压消息|compact…轮后处理|afterTurn…子 Agent|prepareSubagentSpawn/onSubagentEnded…销毁|dispose" |
| 要点2：五种策略方向：legacy、RAG、分层存储、任务感知、自定义压缩 | 已核实 | 13232 回答重点第6段："1. 默认的 legacy 策略…2. 基于检索的 RAG 策略…3. 分层存储策略…4. 任务感知策略…5. 自定义压缩策略" |
| 要点3：ownsCompaction: true 禁用 Pi 内置 auto-compaction，由引擎自主管理；Runner 溢出时强制 compact() 兜底 | 已核实 | 13232 面试官追问第3问："设了 ownsCompaction: true 之后，Pi 的内置 auto-compaction 会被禁用…但不管设没设这个标记，Runner 还有一层溢出压缩兜底" |
| 要点4：legacy 引擎 ingest 是 no-op（SessionManager 负责），RAG 引擎 ingest 需接管向量化和入库 | 已核实 | 13232 面试官追问第1问："legacy 引擎的消息持久化是 SessionManager 在做…换成 RAG 引擎的话，ingest 就得真正接管消息的处理了，至少要做向量化和入库" |
| 要点5：不同引擎降级策略差异：legacy 暴力砍、RAG 少捞几条、分层优先砍冷数据、任务感知按权重动态决定 | 已核实 | 13232 面试官追问第2问："legacy 引擎很暴力，直接从最早的消息开始砍…RAG 引擎好一些，预算紧就少捞几条，质量衰减比较平滑…分层存储引擎会优先砍冷数据…任务感知引擎最灵活，可以根据任务权重动态决定" |

---

## 如果一个 Agent 系统要同时接入 Telegram、飞书、钉钉等渠道，你会怎么设计抽象层？

| 笔记条目 | 状态 | 正文依据 |
|---------|------|---------|
| 要点1：适配器模式+三层架构：统一消息模型→适配器协议→能力声明 | 已核实 | 13237 回答重点第1-4段："第一层：统一消息模型…第二层：适配器协议…第三层：能力声明" |
| 要点2：ChannelPlugin 接口拆成多个维度：id/meta、capabilities、outbound、gateway、status、groups、mentions、agentTools、config | 已核实 | 13237 扩展知识-OpenClaw 的 ChannelPlugin 设计："1）id 和 meta 负责标识…2）capabilities 做能力声明…3）outbound 负责发消息，gateway 负责收消息…4）status 做探活和审计，groups 管群组行为，mentions 处理 @ 提及，agentTools 注入渠道特有工具…5）config 做配置解析和校验" |
| 要点3：ChannelDock 是轻量元数据层，Gateway 路由判断时只需加载元数据 | 已核实 | 13237 扩展知识-轻量元数据层的设计："在完整的 Channel Plugin 之上抽了一层更轻量的元数据层（ChannelDock）。Gateway 做路由和策略判断时…不需要把整个渠道插件都加载进来…这是按需加载思想在插件架构上的体现" |
| 要点4：限流必须放各自 adapter 内部，Telegram 按 bot 全局、Discord 按 channel、Slack 按 workspace | 已核实 | 13237 面试官追问第3问："限流逻辑必须放在各自的 adapter 内部，不能放在统一层，因为各渠道的限制维度不一样。Telegram 是按 bot 全局限流，Discord 是按 channel 限流，Slack 是按 workspace 限流" |
| 要点5：跨渠道上下文打通需用户身份映射层，OpenClaw 目前按渠道隔离 | 已核实 | 13237 面试官追问第4问："如果要打通，需要一个跨渠道的用户身份映射层…OpenClaw 目前选择的是按渠道隔离" |

---

## 同一个系统里可能有多个 Agent，不同渠道用户群组的消息需要路由到不同的 Agent

| 笔记条目 | 状态 | 正文依据 |
|---------|------|---------|
| 要点1：声明式规则+优先级匹配，8 级：peer→peer.parent→guild+roles→guild→team→account→channel→default | 已核实 | 13256 回答重点第1-2段："OpenClaw 的路由匹配优先级从高到低一共 8 级：1）binding.peer…2）binding.peer.parent…3）binding.guild+roles…4）binding.guild…5）binding.team…6）binding.account…7）binding.channel…8）default" |
| 要点2：路由结果含 agentId、sessionKey、mainSessionKey、matchedBy，路由和会话隔离联动 | 已核实 | 13256 扩展知识-路由结果不只是 agentId："这个函数返回的不仅仅是一个 agentId。它还会计算出 sessionKey…mainSessionKey…以及 matchedBy…路由和会话隔离是联动的" |
| 要点3：8 级匹配每级几微秒，消息完整处理 1-10 秒，路由开销可忽略 | 已核实 | 13256 面试官追问第2问："8 级优先级匹配，每级就是一次 Map.get 或者简单的字段比对，加起来也就几微秒。一条消息从网络进来到 LLM 响应完，耗时量级是 1-10 秒，路由匹配的开销可以忽略不计" |
| 要点4：同一优先级配两条规则命中同一消息是配置 bug，应在校验阶段拦截 | 已核实 | 13256 面试官追问第1问："正常情况下不应该出现这种冲突…这种情况本质上是配置 bug，应该在配置校验阶段就拦住" |
| 要点5：支持基于内容的路由，可在链中插入 binding.content 级别，高性能需 Aho-Corasick | 已核实 | 13256 面试官追问第3问："要在优先级链里加一个新的级别，比如在 binding.account 和 binding.channel 之间插入一个 binding.content 级别…规则多了之后需要用 Aho-Corasick 之类的多模式匹配算法优化" |

---

## 同一个用户在 Telegram 私聊和 Discord 群组里和 Agent 对话，应该共享会话还是隔离？

| 笔记条目 | 状态 | 正文依据 |
|---------|------|---------|
| 要点1：应该隔离，私聊和群聊语境、隐私要求完全不同 | 已核实 | 13257 回答重点第1-2段："应该隔离。不同渠道、不同聊天上下文的对话必须放在独立的 session 里。群聊和私聊的语境完全是两码事，用户私聊里跟 Agent 说的东西如果混到群聊里去，那就是隐私泄露事故" |
| 要点2：Session Key 格式 agent:{id}:channel:{type}:peer:{id}，四维度编码天然隔离 | 已核实 | 13257 回答重点第3段："Session Key 就像一个'房间号'，把 agentId（哪个 Agent）、channel（哪个渠道）、accountId（哪个 bot 账号）、peer（跟谁对话）四个维度编码到一起" |
| 要点3：dmScope 4 级：main→per-peer→per-channel-peer（默认）→per-account-channel-peer | 已核实 | 13257 扩展知识-私聊场景的隔离粒度控制："1）main 最宽松…2）per-peer 按对话对象隔离…3）per-channel-peer 按渠道加对话对象隔离…这是默认选项…4）per-account-channel-peer 最细粒度" |
| 要点4：Thread 分配独立 session key（拼 :thread:{threadId} 后缀），不同 Thread 上下文完全隔离 | 已核实 | 13257 扩展知识-群组和 Thread 的隔离第2段："OpenClaw 给 Thread 分配了独立的 session key，在原有 key 的基础上拼了 :thread:{threadId} 后缀。所以同一个群组里的不同 Thread 也是隔离的" |
| 要点5：会话隔离只影响运行时上下文加载，不影响数据层面聚合 | 已核实 | 13257 面试官追问第3问："隔离是 session 层面的，只影响 Agent 回复时看到什么上下文，不影响数据层面的聚合。session key 里已经编码了所有维度信息，分析系统只要按 peer 维度做一次聚合查询" |

---

## 同一个工具（比如「执行命令」）在不同场景下应该有不同的权限

| 笔记条目 | 状态 | 正文依据 |
|---------|------|---------|
| 要点1：7 层策略管道：tools.profile→byProvider.profile→全局 allow/deny→Provider 级 allow→Agent 级 allow→Agent-Provider 级 allow→Group 级 allow | 已核实 | 13258 回答重点第2段详细列出 1-7 层，与笔记完全一致 |
| 要点2：策略管道单调递减，后续层只能在前一层结果内缩小，不能反向扩大 | 已核实 | 13258 面试官追问第1问："不能。OpenClaw 的策略管道是逐层收窄的设计，后面的层只能在前面层的结果基础上进一步缩小范围，不能反向扩大" |
| 要点3：deny 优先于 allow，先用 allow 构建候选集，再用 deny 从中剔除 | 已核实 | 13258 面试官追问第2问："deny 优先于 allow。实现上是先用 allow 过滤出候选集，再用 deny 从候选集中剔除" |
| 要点4：Owner-only 工具直接移除而非调用时拒绝，减少攻击面 | 已核实 | 13258 面试官追问第3问："直接移除的话，LLM 压根不知道这个工具的存在，自然不会去调用。从安全角度看，减少信息暴露永远比事后拦截更靠谱" |
| 要点5：子 Agent 默认 deny spawn，放开需配合深度限制（最大嵌套 3 层、并发 ≤5） | 已核实 | 13258 面试官追问第4问："通过 tools.subagents.tools.allow 按需放开…比如允许 spawn 但限制最大嵌套深度为 3 层，或者限制同时存活的子 Agent 总数不超过 5 个" |

---

## 不同的 LLM Provider 对 Tool Schema 的支持不完全一致，你会怎么处理这种差异？

| 笔记条目 | 状态 | 正文依据 |
|---------|------|---------|
| 要点1：核心方案是 Schema 归一化，normalizeToolParameters() 根据目标 Provider 自动清洗转换 | 已核实 | 13259 回答重点第1-2段："工具只用标准 JSON Schema 定义一次，系统根据目标 Provider 自动做清洗和转换…OpenClaw 在 normalizeToolParameters() 里集中处理这些适配逻辑" |
| 要点2：各 Provider 差异：Gemini 不支持 oneOf/anyOf/additionalProperties/patternProperties，xAI 不支持 minLength/maxLength，OpenAI 要求顶层 type: "object" | 可疑 | **部分细节在 13259 原文中无法验证**。13259 回答重点第3段为图片展示，文本中仅提到"各家 Provider 对 JSON Schema 的支持程度天差地别"。扩展知识-踩坑案例中明确提到 Gemini 不支持 oneOf（第98行）和 OpenAI 要求 type: "object"（第100行），但 **Gemini 不支持 additionalProperties/patternProperties 以及 xAI 不支持 minLength/maxLength 这两处细节在 13259 原文中未出现**，该细节出自 13221（第三篇）扩展知识第3段。笔记将两篇原文的信息合并，核心判断正确，但部分 Provider 差异细节的出处标注有误 |
| 要点3：oneOf 展平策略：合并同名字段 properties，required 仅保留所有 variant 中共有的 | 已核实 | 13259 扩展知识-源码解析第2段："展平 union 时通过 mergePropertySchemas() 合并同名属性的 enum 值…只有在所有 variant 里都出现的 required 字段，才会被保留为最终 required" |
| 要点4：30 工具 × 5 Provider = 150 份 Schema，"一次定义、按需转换"避免维护爆炸 | 已核实 | 13259 扩展知识-为什么不让开发者自己适配："假设你有 30 个工具、5 个 Provider，那就是 150 份 Schema 要维护" |
| 要点5：Schema 约束是提示非强制校验，清洗后工具端仍需二次参数校验 | 已核实 | 13259 面试官追问第1问："Schema 约束是给模型'看'的提示信息，模型并不保证 100% 遵守…工具这一侧本身就需要做参数校验" |

---

## Agent 系统中 Hook/中间件模式有什么用？OpenClaw 的 Hook 系统是怎么设计的？

| 笔记条目 | 状态 | 正文依据 |
|---------|------|---------|
| 要点1：Hook 分两层：内部 Hook（字符串事件名，覆盖 command/session/agent/gateway/message）+ 插件 Typed Hook（强类型命名） | 已核实 | 13260 回答重点第3段："内部 Hook，事件类型覆盖 command、session、agent、gateway、message…插件 Typed Hook，提供强类型的 Hook 名：before_model_resolve、before_prompt_build、llm_input" |
| 要点2：Void Hook 并行执行无返回值（观测），Modifying Hook 串行执行可合并数据（修改数据流） | 已核实 | 13260 扩展知识-Hook 的两种执行模式："Void Hook（发射即忘）：多个 handler 并行执行…Modifying Hook（可修改数据）：多个 handler 串行按优先级依次执行" |
| 要点3：priority 数字越大优先级越高，Modifying Hook 按 priority 降序串行执行 | 已核实 | 13260 扩展知识-Hook 的注册和优先级："priority 控制多个 Hook 的执行顺序，数字越大优先级越高…按 priority 从高到低依次执行" |
| 要点4：与 LangChain Callback 区别：LangChain 偏观察，OpenClaw 的 Hook 可直接修改数据流，拦截能力更强 | 已核实 | 13260 扩展知识-和其他框架的 Hook 机制对比："LangChain 的 Callback 更偏向观察和记录，OpenClaw 的 Hook 可以直接修改数据流…拦截能力更强" |
| 要点5：默认 try-catch 包裹，单个报错只打日志继续。更优设计支持 critical: true/false 声明 | 已核实 | 13260 面试官追问第2问："每个 Hook 执行都 try-catch 包住…更好的设计是 Hook 注册时声明 critical: true/false，critical 的 Hook 报错就中断" |

---

## 什么场景下需要多 Agent 协作而不是单个 Agent 解决？OpenClaw 是怎么支持子 Agent（Subagent）的？

| 笔记条目 | 状态 | 正文依据 |
|---------|------|---------|
| 要点1：三种场景：任务天然可并行拆分、异构模型/工具/权限、单 Agent context 装不下 | 已核实 | 13261 回答重点第1-3段："1）任务天然可以拆成并行的独立子任务…2）不同子任务需要不同的模型、工具、权限配置…3）单个 Agent 的 context window 装不下所有信息" |
| 要点2：子 Agent 三步模型：派活（sessions_spawn）→干活（独立 session）→交差（Announce 推模式+幂等 key） | 已核实 | 13261 回答重点第4-6段："第一步：派活…sessions_spawn…第二步：干活…第三步：交差…Announce 机制…推模式…每次回传都带一个唯一的幂等 key" |
| 要点3：两种运行模式：run（执行完销毁）+ session（保持会话，支持后续指令） | 已核实 | 13261 扩展知识-子 Agent 的两种运行模式："1）run 模式…2）session 模式…父 Agent 后续可以通过 /subagents send 继续给它发消息" |
| 要点4：并发控制靠 subagent lane（并发槽位）+ maxSpawnDepth（嵌套深度，默认 1） | 已核实 | 13261 面试官追问第3问："subagent lane 做资源隔离。lane 本质上就是一个并发控制的槽位…如果 lane 的容量设成 5，那最多同时跑 5 个子 Agent…再加上 maxSpawnDepth 限制嵌套深度" |
| 要点5：子 Agent 与父 Agent 完全隔离，上下文通过 prompt 显式传入摘要，强制信息显式传递 | 已核实 | 13261 面试官追问第1问："创建子 Agent 的时候，父 Agent 需要把必要的上下文信息通过 prompt 传过去。这是有意为之的设计，强制做信息的显式传递而不是共享内存…实际使用中一般会把关键的摘要信息传过去，而不是原始数据" |

---

## 多 Agent 之间如何通信和协调？OpenClaw 的 Gateway 在其中扮演什么角色？

| 笔记条目 | 状态 | 正文依据 |
|---------|------|---------|
| 要点1：两条通信路径：Announce 机制（推模式）+ 斜杠命令交互 | 已核实 | 13263 回答重点第1-2段："第一条是 Announce 机制（公告机制）…第二条是斜杠命令交互。通过 /subagents send 可以主动给子 Agent 发消息" |
| 要点2：Gateway 是中央调度器，两级排队保证串行和总量可控 | 已核实 | 13263 回答重点第3段 + 扩展知识-排队与并发控制："Gateway 在这里面扮演的是中央调度器的角色…Gateway 用两级排队解决这个问题：1）Session 级排队…2）全局排队" |
| 要点3：announce 消息携带稳定 idempotency key，Gateway 通过 dedupe map 去重 | 已核实 | 13263 扩展知识-幂等性保证："子 Agent 的 announce 消息用的是稳定的 idempotency key。网络抖动导致重复投递的时候，Gateway 通过 dedupe map 去重" |
| 要点4：Gateway 维护三种运行时状态：dedupe map、agentRunSeq、chatRunState | 已核实 | 13263 扩展知识-Runtime State 管理："1）dedupe map 做请求去重…2）agentRunSeq 是 Agent 运行序列号…3）chatRunState 维护聊天的运行状态" |
| 要点5：父 Agent 不依赖到达顺序，按任务 ID 匹配处理，必要时结合 agentRunSeq 自行排序 | 已核实 | 13263 面试官追问第3问："不要依赖到达顺序。每个子 Agent 的 announce 消息里带上任务 ID 和上下文信息，父 Agent 按任务 ID 匹配处理…OpenClaw 的 Gateway 通过 agentRunSeq 提供了顺序信息" |

---

## OpenClaw 采用插件架构，设计一个插件系统需要考虑哪些关键问题？

| 笔记条目 | 状态 | 正文依据 |
|---------|------|---------|
| 要点1：四个核心问题：注册与发现（jiti JIT 加载，extensions/ 目录）、隔离性（包结构隔离，peerDependencies）、API 稳定性（plugin-sdk 入口）、扩展点粒度 | 已核实 | 13264 回答重点第1-4段："1）注册与发现…jiti…2）隔离性…peerDependencies…3）API 稳定性…plugin-sdk 入口收敛暴露面…4）扩展点的粒度设计…每种能力都有独立的注册入口" |
| 要点2：7 种注册方法：registerTool、registerChannel、registerHook/on、registerProvider、registerHttpRoute、registerService、registerCommand | 已核实 | 13264 回答重点第4段表格，逐一列出 7 种注册方法 |
| 要点3：PluginRegistry 是 IoC 核心：插件向 Registry 注册，核心模块通过 Registry 查询 | 已核实 | 13264 扩展知识-PluginRegistry："PluginRegistry 是一个全局收集器…这是典型的 IoC（控制反转）模式：核心不依赖插件，插件反过来向核心注册自己" |
| 要点4：插件可通过 Registry 间接访问其他插件能力（api.getRegisteredTools()），但禁止直接 import | 已核实 | 13264 面试官追问第4问："可以，但只能通过 Registry 间接访问，不能直接 import 另一个插件的代码…通过 api.getRegisteredTools() 拿到工具列表" |
| 要点5：配置兼容靠 safeParse 做迁移，最佳实践：新字段给默认值、老字段标记 deprecated 保留一两个版本 | 已核实 | 13264 面试官追问第3问："得靠插件作者在 safeParse 里做兼容处理…最佳实践是只做增量变更：新字段给默认值，老字段标记 deprecated 继续兼容一两个版本再移除" |

---

## OpenClaw 的 Gateway 对 Agent 请求做了幂等性处理

| 笔记条目 | 状态 | 正文依据 |
|---------|------|---------|
| 要点1：idempotencyKey（UUID）+ Gateway 内存 dedupe Map，缓存有 TTL 和 DEDUPE_MAX | 已核实 | 13265 回答重点第2段："通过 idempotencyKey + 内存 dedupe Map 来做幂等。每个请求带一个 UUID 作为幂等键…缓存有 TTL 和最大条目数限制" |
| 要点2：策略是"防止重复触发，不做回滚"，已执行的副作用保留 | 已核实 | 13265 回答重点第3段："OpenClaw 的策略是防止重复触发，不做回滚。已经执行的副作用就让它留着，重点保证后续不会再重复执行" |
| 要点3：消息发送三层防护：dedupe Map + inflightMap + transcriptHasIdempotencyKey() | 已核实 | 13265 扩展知识-消息发送的双层防护：原文明确描述三层——dedupe（历史上发过没）、inflight（当前正在发）、transcriptHasIdempotencyKey()（对话历史中是否已存在） |
| 要点4：缓解不可幂等工具副作用三层：优先设计幂等工具、工具侧去重、接受"至多一次"语义 | 已核实 | 13265 扩展知识-处理已产生副作用的策略："1）让工具本身尽量设计为幂等的…2）对不可幂等的工具，在工具侧做去重…3）接受'至多一次'语义。宁可漏执行也不重复执行" |
| 要点5：内存 dedupe 重启丢失，单实例风险可控；分布式需 Redis SETNX 或一致性哈希 | 已核实 | 13265 面试官追问第1-2问："对于 OpenClaw 这种单实例部署的场景，重启窗口很短，实际风险不大…单实例的内存 Map 搞不定多实例场景…这时候得换成集中式存储，比如 Redis 的 SETNX…或者在请求入口加一致性哈希" |

---

## 如果让你基于 OpenClaw 的设计理念从零搭建一个 Agent 框架，你会先做哪三个模块？

| 笔记条目 | 状态 | 正文依据 |
|---------|------|---------|
| 要点1：三个模块优先级：Agent Runner→Context Engine→Gateway | 已核实 | 13266 回答重点第1-3段："从零搭一个 Agent 框架，我会先做三件事：Agent Runner（执行引擎）、Context Engine（上下文管理）、Gateway（请求网关）。这三个是最小可运行系统的核心" |
| 要点2：Agent Runner 关键设计：模型抽象层（LlmClient 接口）、声明式工具注册、错误处理和超时第一版就搭好 | 已核实 | 13266 扩展知识-Agent Runner 的核心设计："1）模型抽象层要做好…接口定义成 LlmClient…2）工具注册要用声明式…3）错误处理和超时不能拖到后面再加。从第一版就要把 timeout 和 retry 机制搭好" |
| 要点3：Context Engine 核心矛盾：token 窗口有限 vs 对话历史越来越长，分层管理：最近原文保留、更早 compaction 摘要、System Prompt 和工具描述单独管理 | 已核实 | 13266 扩展知识-Context Engine 的设计要点："1）最近几轮的对话原样保留…2）更早的历史通过 compaction 压缩成摘要…3）系统 prompt 和工具描述单独管理，这部分每次都要完整带上，不能裁" |
| 要点4：Gateway 六大类职责：消息渠道管理、会话管理与排队、幂等去重、Cron、子 Agent 协调、配置热更新 | 已核实 | 13266 扩展知识-Gateway 的职责全景："1）消息渠道管理…2）会话管理与请求排队…3）幂等去重…4）定时任务（Cron）…5）子 Agent 协调…6）配置热更新与运维" |
| 要点5：LLM 返回不存在工具名时构造 tool_result 告知"工具不存在，请重新选择"，连续 3 次调错终止 | 已核实 | 13266 面试官追问第1问："直接构造一条 tool_result 消息告诉 LLM '这个工具不存在，请重新选择'，然后继续循环让 LLM 重新决策…一般加个最大重试次数，比如连续 3 次都调错就终止" |

---

## 核查统计

| 状态 | 条数 | 占比 |
|------|------|------|
| 已核实 | 123 | 98.4% |
| 可疑/原文无依据 | 2 | 1.6% |
| **合计** | **125** | **100%** |

### 可疑条目汇总

| 篇目 | 条目 | 问题描述 |
|------|------|---------|
| 7. OpenClaw 是什么？ | 要点3 | "127.0.0.1:18789" 端口细节出自 13216（第一篇），非 13225（第七篇）原文。13225 中仅提到"本地运行""数据在本地"，未提及绑定地址。核心语义正确，出处标注有误。 |
| 19. 不同 LLM Provider 对 Tool Schema 的支持 | 要点2 | "Gemini 不支持 additionalProperties/patternProperties" 和 "xAI 不支持 minLength/maxLength" 两处细节出自 13221（第三篇），非 13259（第十九篇）原文。13259 中仅提及 Gemini 不支持 oneOf 和 OpenAI 要求 type: "object"。核心判断正确，部分细节出处标注有误。 |

### 核查结论

笔记整体质量较高，125 条要点中 123 条与原文完全对应，数字、结论、技术细节、引用路径均准确。2 条"可疑"均为跨篇信息合并导致的出处标注偏差，所陈述的事实本身在原始资料中均有依据（只是出处标注到了错误的篇目），不影响笔记内容的正确性。建议修订时调整这两条的出处说明。