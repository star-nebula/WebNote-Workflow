# 00-采编清单

> 生成时间：2026-09-03
> 执行者：调度器（主会话，采编步骤）
> 运行模式：模式 B（单对话框降级）——本工具环境无 subagent 创建能力（已核验 ToolSearch 无 Agent/Task/Explore/Generalist 类工具），按 Webpage note workflow.md「执行模式」说明降级处理
> 输入批次：端到端验收 · 6 条内容（p01–p06）
> 输出目录：`runs/2026-09-03-端到端验收/`

## 内容清单

| ID | 标题 | 域名 | 媒体格式 | 正文字符数 | 有正文 | 提取方式 | 提取结果 | 原文来源 |
|----|------|------|---------|-----------|--------|---------|---------|---------|
| p01 | Git 提交信息规范实践 | blog.example.org | 图文（已有正文） | 807 | 有 | 直接使用剪藏正文 | 成功 | `<示例素材>/webpage-1-git-convention.md` |
| p02 | 久坐族的脊柱健康：几个被低估的细节 | health.example.org | 图文（已有正文） | 534 | 有 | 直接使用剪藏正文 | 成功 | `<示例素材>/webpage-2-spine-health.md` |
| p03 | 某开源项目发布 v2.0：引入插件化架构 | news.example.org | 图文（已有正文） | 325 | 有 | 直接使用剪藏正文 | 成功 | `<示例素材>/webpage-3-oss-news.md` |
| p04 | MarkdownLint 工具使用说明 v1.4 | -（本地文档） | 文档（.docx） | 486 | 有 | `extract-document.py`（python-docx） | 成功 | `<示例素材>/markdownlint-manual.docx` |
| p05 | 某漫剧制作教程的一帧截图 | -（本地图片） | 图片 | 579 | 有 | `extract-image-ocr.py`（RapidOCR）+ `ds_vision.py --mode image`（DeepSeek Vision） | 成功 | `<示例素材>/frame-p05.jpg` |
| p06 | 干货漫剧制作教程（从 0 到上架一个视频） | bilibili.com | 视频（本地 mp4） | 15312 | 有 | `extract-video-frames.py --t-start 100 --t-end 220`（抽帧 + RapidOCR + DeepSeek Vision） | 成功 | `<示例素材>/干货漫剧制作教程，从0到上架一个视频说完.mp4` |

## 逐条采编说明

### p01 · Git 提交信息规范实践
- 来源类型：网页图文剪藏（用户已存正文）
- 处理方式：无需工具，直接读取剪藏 md 正文
- 内容要点：提交信息三段结构（summary/body/footer）、约定式提交 `<type>(<scope>): <subject>`、type 清单（feat/fix/docs/refactor/chore/test）、为什么值得做（changelog/bisect/review）、常见误区

### p02 · 久坐族的脊柱健康：几个被低估的细节
- 来源类型：网页图文剪藏（用户已存正文）
- 处理方式：无需工具，直接读取剪藏 md 正文
- 内容要点：腰椎前凸弧度、坐姿要点、椅子支撑位置、45–60 分钟起身一次、屏幕/键鼠高度

### p03 · 某开源项目发布 v2.0：引入插件化架构
- 来源类型：网页图文剪藏（用户已存正文）
- 处理方式：无需工具，直接读取剪藏 md 正文
- 内容要点：v2.0 插件化、插件市场、YAML→TOML 迁移、启动时间缩短 40%、1.x 2027 停维护、社区反响

### p04 · MarkdownLint 工具使用说明 v1.4
- 来源类型：本地 .docx
- 处理方式：`python tools/extract-document.py <path>` → method `python-docx`，success=true
- 内容要点：安装、基本用法、MD001/MD013/MD024 规则、.markdownlint.jsonc 配置、FAQ

### p05 · 某漫剧制作教程的一帧截图
- **前置检查记录**：用户消息指定源图路径 `frame-p05.jpg`，但**该文件不存在**；夹具目录下亦无任何 .jpg（已在全盘候选位置检索）。用户补充说明此图是 p06 视频中某段关键画面（含界面文字）。
- **自主决定**：按任务书授权"最合理默认"，用 ffmpeg 从 p06 源视频 `205s` 处抽取一帧写入夹具目录 `frame-p05.jpg`（该帧含 DeepSeek「AI漫剧分镜创作准备」界面文字），作为 p05 的原文来源；并如实记录此替代（非原截图）。
  - 命令：`ffmpeg -y -ss 205 -i "<示例素材>/干货漫剧制作教程，从0到上架一个视频说完.mp4" -frames:v 1 -q:v 2 "<fixtures>/frame-p05.jpg"`
- 处理方式（image-extract 方法论）：
  1. `extract-image-ocr.py --images frame-p05.jpg`（本地 RapidOCR，逐字文字）
  2. `ds_vision.py frame-p05.jpg --mode image`（DeepSeek Vision，画面语义）
- **工具问题记录**：`extract-image-ocr.py` 的 `--images` 分支在 Windows 跨盘符时崩溃——`os.path.relpath` 在图片在 C: 盘、cwd 在 E: 盘时抛 `ValueError: path is on mount 'C:', start on mount 'E:'`（extract-image-ocr.py:107）。规避：先在 C 盘目录下执行再指回脚本路径。
- OCR 与 Vision 均成功，文字 + 语义完整。

### p06 · 干货漫剧制作教程（从 0 到上架一个视频）
- 来源类型：本地视频（16 分钟，976s），无页面文本可提取（URL 为占位，用户明确"无需 BrowserSkill、本地处理"）
- 处理方式（video-extract 方法论，用户授权抽样 100–220s）：
  - `python tools/extract-video-frames.py --input "<video>" --t-start 100 --t-end 220 --keep-frames --out p06_frames.json`
  - 链路：ffmpeg 抽帧（5s 周期 + 场景帧）→ 本地 RapidOCR 逐帧文字 → DeepSeek Vision 每 14 帧一批语义
  - 结果：24 帧成功，per_frame 含 `t / ocr_text / vision_text`；总 markdown 输出已存 JSON。
- 该区段覆盖：选题方法（红果热播榜找大热漫剧仿写）→ 用 DeepSeek 当编剧生成方向与 30 集细纲 → 角色/分镜/提示词撰写师角色分工 → 片段拆分规范与视频生成 Prompt 示例。
- **未执行的链路步骤（如实记录）**：
  - `extract-video-text.py <URL>`：视频 URL 为占位符（bilibili.com/video/example），且本次按"本地文件、无需 BrowserSkill"处理，未执行页面文本提取。
  - `extract-video-asr.py`（语音转写）：用户明确授权"只看 100–220s 画面内容以节省时间"，故本次未做 ASR（非工具失败，是授权范围决定）。若要听全片口播内容，需另行跑 ASR。

## 分类汇总（供 01 使用）

- 有正文：p01–p06 共 6 条
- 无正文：0 条

## 执行模式判定记录

- 尝试创建 subagent（归类师/笔记匠/核查官）→ **失败/不可用**：本会话工具清单中不存在 Agent/Task/Explore/Generalist/AskUserQuestion 等可创建独立上下文的工具（已通过 ToolSearch 检索验证）。
- 按 Webpage note workflow.md 第 44–45 行 / SKILL.md 第 42–44 行降级为**模式 B（单对话框分段）**：采编/归类/提炼在同一对话完成；核查原则上应在新对话执行——**本次无人值守单会话无法开新对话，故 03 在同会话内以"独立自查口径"完成并显式标注降级性质**（详见 03 文件头）。
