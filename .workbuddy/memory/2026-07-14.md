# 2026-07-14

## 生成发布用内容
- 为 WebNote-Workflow 项目生成两份对外发布内容（按需分拆，未合并）：
  - `README.md`：技术参考文档，含中英文简介、四步流程说明、目录结构、三种用法、确认边界。wikilink 已转为标准相对 Markdown 链接，适配 GitHub 渲染。
  - `posts/网页笔记四步工作流.md`：博客文章（标题/摘要/正文/小结叙事结构），面向 CSDN/掘金/个人站，未引用仓库内其他文件，末尾引导至 GitHub。
- 判断依据：README 是结构化参考文档，博客是叙事性文章，用途差异大，故分拆为两份而非合并。
- 注：仅生成文件，未执行 git 提交/推送到 GitHub，也未实际发布到博客平台（属外部动作，待用户确认）。

## 仓库配置：排除本地运行产出
- 用户已 `git init` 并有一个含 runs 内容的 commit（db6294a）。按要求不将 `runs/` 内容发布到云端。
- 新增 `.gitignore`：忽略 `runs/*`（保留 `runs/.gitkeep` 以留空目录结构）、忽略 `.workbuddy/`、系统杂项。
- 执行 `git rm --cached -r runs/` 后重新 `git add runs/.gitkeep`，提交 5817b07。本地 runs 实际文件仍在磁盘、未被删除，仅不再进仓库索引。
- 现状：推送时不会包含任何 runs 实际内容，仅保留空目录占位。尚未 push（外部动作，待用户授权）。
