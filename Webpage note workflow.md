# 网页笔记工作流

把"内容采集 → 整理分类 → 提炼笔记 → 确认发布"拆成**调度器（主会话） + 三个独立 Agent** 分工协作。

不是每一步都需要独立的 AI 上下文。确定性的事调度器（主会话）自己干，只有真正需要独立判断的才交给 subagent。

## 架构总览

```
调度器（主会话）
├── 1. 采编（主会话自己做——确定性逻辑）
│      URL 正则分类 + 调用 tools/extract-*.py 提取文本
│      → 产出 00-采编清单.md
│
├── 2. 创建 subagent：归类师 ← 独立上下文
│      读正文做四选一语义分类
│      → 产出 01-整理清单.md
│
├── 3. 创建 subagent：笔记匠 ← 独立上下文
│      读正文提取 3~5 条核心要点
│      → 产出 02-笔记.md
│
├── 4. 创建 subagent：核查官 ← 全新上下文，必须独立
│      逐条比对原文 vs 笔记，专抓 AI 编造
│      → 产出 03-核查表.md
│
└── 5. 确认（主会话自己做——汇总+约束）
       读前三道产出汇总提案
       → 产出 04-待确认提案.md（全部停在"待确认"）
```

**为什么有些步骤不是独立 Agent：**

| 步骤 | 是否独立 | 理由 |
|------|---------|------|
| 采编 | ❌ 主会话内化 | URL 分类用正则，文本提取调用 CLI 工具——都是确定性逻辑，不需要 AI。AI 做反而可能猜错 |
| 归类师 | ✅ 独立 | 四选一分类需要理解正文语义，归类师的判断不应影响笔记匠 |
| 笔记匠 | ✅ 独立 | 提炼是核心创作任务，需要完整上下文专注正文 |
| 核查官 | ✅ **必须独立** | 这是安全闸门。笔记匠自己核查自己等于自检——它查不出自己刚才编了什么 |
| 确认 | ❌ 主会话内化 | 不读原文、不做新判断，只是汇总前三个产出 + 约束落地行为。确定性逻辑 |

---

## 调度流程（主会话的职责）

### 第一步：采编（主会话自行执行）

调度器（主会话）根据 URL 做媒体格式分类，并调用对应工具提取文本。**这一步不需要创建 subagent，调度器（主会话）自己干。**

**媒体格式判断规则**（优先级由高到低）：
1. **URL 文件扩展名**：取 URL 路径末尾（不含查询参数）判断是否以 .pdf/.docx/.pptx/.xlsx 等文档扩展名结尾 → 文档
2. **URL 域名 + 路径模式**：
   - 音频：music.163.com、music.youtube.com、spotify.com、xiaoyuzhoufm.com、podcasts.apple.com
   - 视频：youtube.com/watch、youtu.be、bilibili.com/video、b23.tv、douyin.com、kuaishou.com、v.qq.com、v.youku.com、iqiyi.com
   - 图文：mp.weixin.qq.com、zhuanlan.zhihu.com、blog.csdn.net、juejin.cn、oschina.net、segmentfault.com、medium.com
3. **URL 路径模式**：URL 路径含 /article/、/posts/、/blog/ 等常见文章路径指向图文
4. **无法判断时标「无法判断」**，不硬猜

**各媒体格式的提取策略：**

| 媒体格式 | 优先工具 | ASR / 画面回退 | 备注 |
|---------|---------|---------|------|
| 图文（已有正文） | 直接使用 | - | 用户已存好的正文透传 |
| 图文（正文为空） | `tools/extract-webpage.py <URL>` | - | 通过 BrowserSkill 读取页面 |
| 视频 | `tools/extract-video-text.py <URL>` | `tools/extract-video-asr.py <URL>`（语音）→ `tools/extract-video-frames.py --input <本地>`（画面） | 先页面文本；不足时 ASR 转语音；**教程/录屏类画面含文字时再用画面采集** |
| 音频 | `tools/extract-video-text.py <URL>` | `tools/extract-video-asr.py <URL>` | 先取页面 shownotes；不足时用 ASR |
| 文档 | `tools/extract-document.py <文件路径>` | - | 支持 PDF/DOCX |
| 无法判断 | `tools/extract-webpage.py <URL>` | - | 尽力读取 |

**视频/音频的三步策略**：
1. 先用 `extract-video-text.py` 读取页面文本（标题、描述、评论）——秒级，零依赖
2. 如果页面文本不足（如字符数 < 200），用 `extract-video-asr.py` 下载音频并 ASR 转写——分钟级，需模型
3. **画面采集（可选）**：若视频为**教程/课程/录屏/演示类**（画面含提示词原文、界面文字、操作步骤等语音转写覆盖不到的内容），对本地视频跑 `extract-video-frames.py --input <视频>`，抽帧后本地 RapidOCR 提取逐字文字、云端 DeepSeek Vision 理解画面语义，按时间戳对齐输出。

> **画面采集触发规则（重要）**：Agent 无法判断某视频是否属于"教程/录屏类、画面含文字"时，**必须询问用户**，不得擅自决定执行或跳过。只有能确定画面含有效文字信息时才自动启用第三步；否则停在第二步。

**工具输出格式**（统一 JSON 到 stdout）：

```json
{"success": true, "text": "提取到的文本...", "char_count": 1234, "method": "pypdf|bsk-webpage|bsk-video|faster-whisper-small|video-frames-rapid+ds-vision", "error": null}
```

> `extract-video-frames.py` 另输出扩展字段 `per_frame[]`（每帧 `file / t / ocr_text / vision_text`），用于按时间戳对齐画面语义与逐字文字。

**铁律**：
- 通过 BrowserSkill 操作**用户已登录的浏览器**读取页面，不发起未经认证的 HTTP 请求
- 只提取已有文本，不生成、不总结、不补充
- 提取失败时如实标"无法提取"，不硬凑
- `extract-video-frames.py` 需要 `DEEPSEEK_API_KEY` 环境变量（云端 Vision）；Key 不写进代码、不提交 git

**采编输出 → `00-采编清单.md`**

每条内容包含：标题 | 域名 | 媒体格式 | 正文字符数 | 有正文 | 提取方式 | 原文来源

---

### 第二步：创建 subagent · 归类师

**角色**：内容分类员。

**负责**：从采编清单读正文，做初步四选一语义分类。

**输入**：调度器（主会话）提供的采编清单（含标题、域名、正文、媒体格式等）。

**输出**：
- 标题 + 域名
- 正文字符数 + 有无正文（有 / 无）
- 原文来源
- 媒体格式（来自采编清单，转录即可）
- 初步四选一分类：
  - **学习**：有能直接上手的技术、方法或知识
  - **借鉴**：写法、结构、表达方式值得参考
  - **内容补充**：能给我手头某个选题补事实或数据
  - **可归档**：信号低、已经掌握、或重复

**铁律**：
- 正文是采编提取好的，不要自己去抓网页，也不要凭标题脑补
- 没有正文就如实写"无"，绝不拿标题硬凑分类
- 媒体格式来自采编清单，如实转录
- 这一步只归类，不提炼、不下结论

**输出 → `01-整理清单.md`**

---

### 第三步：创建 subagent · 笔记匠

**角色**：笔记提炼员。

**负责**：从归类师输出的正文中提取要点，整理成结构化个人笔记。

**输入**：调度器（主会话）提供的整理清单中有正文的页面内容。

**输出**：
- 3~5 条核心要点（每条标注正文出处位置）
- 一句"为什么值得保留"
- 修订建议：值得深读 / 参考写法 / 补充数据 / 可忽略

**铁律**：
- 只依据正文。正文里没有的数字、结论一律不写
- 每个具体数字、结论都标出它在正文里的位置
- 标题常常比正文激进，不要照标题推断内容
- 你产出的是笔记，不是判决。值不值得留你给建议，最终由用户定

**输出 → `02-笔记.md`**

---

### 第四步：创建 subagent · 核查官（必须全新上下文）

**角色**：笔记核查员。

**负责**：拿笔记匠提炼的每条要点回到原文逐条比对。**专抓 AI 编造的内容。**

**这是整套里最容易被跳过、也最关键的一道。** AI 提炼东西的时候会一脸自信地编。没有这一道，一条假数字就会被你当真存进知识库。

**铁律：核查必须独立，不能自己查自己。** 核查官必须是一个全新的 Agent、一份干净的上下文，和笔记匠不是同一个。

**输入**：整理清单（含原文路径，只读） + 笔记。

**输出**：

| 笔记条目 | 状态 | 正文依据 |
|----------|------|----------|
| 每条要点 | 已核实 / 可疑 | 原文出处位置或冲突说明 |

**铁律**：
- 只依据原文判断，不用自身知识帮笔记圆场
- 原文没写的，就是没写
- 全新 Agent，不为笔记匠辩护

**输出 → `03-核查表.md`**

---

### 第五步：确认（主会话自行执行）

调度器（主会话）读前三个产出，汇总成待确认提案。**不创建 subagent，调度器（主会话）自己干。**

**负责**：
- 合并归类师、笔记匠、核查官的产出
- 对每条笔记要点给建议动作（保留 / 修正 / 删除）
- 核查标"可疑"的条目单独列出，等用户逐条核对
- **全部停在"待确认"，不执行任何落地动作**

**铁律**：
- 不重新核查原文（核查已经做完了）
- 拿不准时宁可多留一条让用户决定，也不替用户删
- 所有删除、归档、发布、写入长期记录的动作都是提案，不自动执行

**输出 → `04-待确认提案.md`**

---

## 工具链

```
你 → BrowserSkill / 剪藏工具 / 手动复制 → 原始内容（URL + 可能有的正文）
                                                    │
               调度器（主会话） → 采编（正则 + tools/*.py）
                                                    │
               归类师（独立 subagent）→ 四选一分类
                                                    │
               笔记匠（独立 subagent）→ 提炼要点
                                                    │
               核查官（全新 subagent）→ 逐条核查
                                                    │
               调度器（主会话） → 确认（汇总提案，全部待确认）
```

**tools/ 目录下的工具：**

| 工具 | 用途 | 依赖 |
|------|------|------|
| `tools/extract-document.py <文件路径>` | 从 PDF/DOCX 提取文本 | pypdf, python-docx |
| `tools/extract-webpage.py <URL>` | 通过 BrowserSkill 读取图文页面 | bsk CLI |
| `tools/extract-video-text.py <URL>` | 通过 BrowserSkill 读取视频页面文本 | bsk CLI |
| `tools/extract-video-asr.py <URL>` | 下载视频音频并用 ASR 转写语音 | yt-dlp, faster-whisper, ffmpeg |
| `tools/extract-video-frames.py --input <视频>` | 视频画面采集：抽帧 + 本地 RapidOCR 取文字 + 云端 DeepSeek Vision 理解语义 | ffmpeg, rapidocr-onnxruntime, DEEPSEEK_API_KEY |
| `tools/ds_vision.py <图片...>` | 单批图片送 DeepSeek Vision 理解（被 extract-video-frames 内部调用） | DEEPSEEK_API_KEY |

---

## 确认边界

AI 出提案，你按确认键。判断标准：**这个动作如果错了，撤得回来吗？**

### 必须停在"待确认"的动作

| 动作 | 为什么危险 | 应该怎样 |
|------|-----------|---------|
| 删除任何资料、笔记、文件 | 删了找不回来 | 只标"建议删除"，你点头才删 |
| 归档 / 收起 / 移走 | 眼不见就再也不翻 | 只标"建议归档"，你确认 |
| 发布（发文章、发社交媒体） | 发出去全网看到 | 出草稿，你过目才发 |
| 改长期记忆 / 知识库 / 笔记 | 一条假信息进了长期库 | 标出来，你核对才写 |
| 改文件名 / 大批移动文件 | 一动一大片，引用全断 | 列清单，你确认才动 |

### 可以让调度器（主会话）直接干的

- 媒体格式分类（正则匹配 URL）
- 调用 tools/*.py 提取文本
- 读、归类、提要点、列清单、出提案
- 在临时草稿里改来改去
- 给建议、做对比、标可疑

**一句话：AI 可以无限地"想"和"建议"，但凡要"落地动手"，停在你这道闸前。**

---

## 跨工具使用

### 方式一：调度器（主会话）自动执行（推荐）

把本工作流文档作为系统提示提供给 AI 工具。AI 会自动：
1. 执行采编（主会话内化）
2. 创建归类师 subagent
3. 创建笔记匠 subagent
4. 创建核查官 subagent（全新上下文）
5. 执行确认（主会话内化）

### 方式二：只用对话框

开三个对话框，分别粘贴归类师、笔记匠、核查官的提示词。采编和确认由你手动完成。

第一段（本对话）：调度器（主会话）先做采编 → 粘贴归类师提示词产出 01 → 粘贴笔记匠提示词产出 02 → **停下**
第二段（新对话）：粘贴核查官提示词产出 03
第三段：调度器（主会话）读 01+02+03 汇总产出 04-待确认提案

**核查那道必须另开新对话。** 别在笔记匠那个对话里接着问。

### 方式三：文件流水线

每次运行在 `runs/` 下建子目录 `YYYY-MM-DD-主题`，五个输出文件并列写入：

| 步骤 | 产出文件 | 执行者 |
|------|---------|--------|
| 采编 | `00-采编清单.md` | 调度器（主会话） |
| 归类师 | `01-整理清单.md` | subagent |
| 笔记匠 | `02-笔记.md` | subagent |
| 核查官 | `03-核查表.md` | subagent（全新上下文） |
| 确认 | `04-待确认提案.md` | 调度器（主会话） |

原文不复制、不移动，仅在采编清单和整理清单中记录来源路径。多次运行互不覆盖。

---

## 一句话纪律

**AI 可以无限地"想"和"建议"，但凡要"落地动手"，停在你这道闸前。**

---

> 设计参考：[Agent OS 的第一课：会分工，才会用 Agent](https://www.axtonliu.ai/newsletters/ai-2/posts/agent-os-workstation-division)（分工隔离、独立核查、确认闸门三层设计）
> 工具依赖：BrowserSkill（浏览器操作）见 [[BrowserSkill Installation Guide|BrowserSkill 安装指南]]，命令参考见 [[BrowserSkill Command Reference|BrowserSkill 命令参考]]
