# 网页笔记工作流

把"存好正文 → 整理分类 → 提炼笔记 → 确认发布"拆成四个 Agent 分工协作。你丢给任何一个 AI 工具（WorkBuddy、Codex、Claude Code），它都能自动创建这四个 Agent 并完成任务。

## 调度流程（调度器的职责）

在四个 Agent 上场之前，调度器先做三件事：

1. **创建四个独立 subagent**：按下方 Agent 1~4 的定义创建，每个拥有独立上下文
2. **理解用户意图**：确认用户要处理什么、主题是什么、有没有特殊要求
3. **向用户索要内容**：告诉用户"请提供需要处理的文件或文件路径"，拿到内容后再启动流水线

**不要在创建 subagent 之前就要内容。** 先搭好流水线，再开闸放水。

拿到内容后，在 `runs/` 下建子目录 `YYYY-MM-DD-主题`，四个输出文件并列写入：`01-整理清单.md` → `02-笔记.md` → `03-核查表.md` → `04-待确认提案.md`。

---

## 为什么拆成四个

**分工不是为了显得高级，是为了隔离上下文、隔离责任、隔离风险。**

- 提炼的不能自己核查自己——它查不出自己刚才脑补了什么
- 核查的不能替你删东西——它只标"可疑"，不替你决定
- 确认的只出提案、不自动落地——最后那个确认键一直在你手里

一个 Agent 从头干到尾，AI 编的内容你根本发现不了。四个 Agent 各管一段，每一段都有独立上下文，"想包庇自己都做不到"。

> **什么时候该拆、什么时候不该拆**：一件事能拆成几条互不干扰的独立子任务时，分工才赢；拆不开的，硬拆只会添乱。这四步（整理 → 提炼 → 核查 → 确认）每一步的产出都能干干净净交给下一步，中间不用反复横跳，所以拆得值。

---

## 工具链

```
你 → BrowserSkill / 剪藏工具 / 手动复制 → 正文已存好
                                                    │
你 → WorkBuddy/Codex/Claude Code → 四个 Agent 接力处理
```

**正文不让 AI 现去抓。** 直接抓十有八九抓不全，还经常失败，付费墙和需要登录的页更抓不到。正文这种又脏又不稳的活，交给成熟的工具：

- **BrowserSkill**：腾讯开源的浏览器桥接，操控你已登录的浏览器读取页面（安装见 [[BrowserSkill Installation Guide|BrowserSkill 安装指南]]）
- **Obsidian 网页剪藏插件**：一键保存到 vault
- **Readwise Reader / 简悦 / Cubox** 等稍后读工具
- **手动复制粘贴**：最稳的方式

正文存好之后，四个 Agent 才上场：整理分类 → 提炼笔记 → 独立核查 → 确认提案。AI 只干它擅长的归类和判断，不干它不擅长的抓网页。

---

## Agent 1 · 整理

**角色**：内容整理与分类员。

**负责**：把用户已经存好的网页正文（连同标题和网址）整理成一张清单，做初步四选一分类。不提炼、不判断对错，就负责"读进来、归类"。

**前置步骤（不是 Agent 干的活）**：用户先用 BrowserSkill、剪藏工具或手动复制，把网页正文存好。正文存不到（如 YouTube、付费墙）的，正文留空。

**输入**：用户提供的内容（标题 + 网址 + 正文）。正文可以是行内文本，也可以是文件路径——调度器会读入文件内容传给 Agent，Agent 只读不改原文。

**输出**：
- 标题 + 域名
- 正文字符数 + 有无正文（有 / 无）
- 原文来源（行内文本 / 文件路径，便于后续核查时定位原文）
- 初步四选一分类：
  - **学习**：有能直接上手的技术、方法或知识
  - **借鉴**：写法、结构、表达方式值得参考
  - **内容补充**：能给我手头某个选题补事实或数据
  - **可归档**：信号低、已经掌握、或重复

**铁律**：
- 正文是工具替你存好的，你不要自己去抓网页，也不要凭标题脑补正文
- 没有正文就如实写"无"，绝不拿标题硬凑一个分类
- 这一步只归类，不要提炼要点、不要下结论，那是下一道的事
- 原文文件只读不改，后续核查需要回到原文比对

**提示词**（复制到新 Agent/对话框）：

```
你是内容整理与分类员。我会给你一批我已经存好的网页内容，
每条是：标题 + 网址 + 已经存好的正文（存不到正文的，正文为空）。

注意：正文是工具替我存好的，你不要自己去抓网页，也不要凭标题脑补正文。

对每个网页：
1. 记下标题和域名。
2. 记下正文字符数，标明有没有正文（有 / 无）。
3. 记下原文来源（行内文本 / 文件路径），便于后续核查时定位原文。
4. 给一个初步四选一分类：
   学习（有能直接上手的技术或方法）
   借鉴（写法、结构值得参考）
   内容补充（能给我手头某个选题补事实或数据）
   可归档（信号低、已经掌握、或重复）

输出一张表：标题 | 域名 | 正文字符数 | 有正文 | 原文来源 | 初步分类。

铁律：没有正文就如实写"无"，绝不拿标题硬凑一个分类。
这一步只归类，不要提炼要点、不要下结论，那是下一道的事。
原文文件只读，不可修改。
```

**自动化调用**：

```
读入用户已存好的这批网页内容（标题 + 网址 + 正文），逐个整理成清单
（标题 / 域名 / 正文字符数 / 有无正文 / 原文来源 / 初步四选一分类），
没正文的如实标"无"、别拿标题硬凑，不要提炼，写到 01-整理清单.md。
原文文件只读，不可修改。
```

---

## Agent 2 · 提炼

**角色**：笔记提炼员。

**负责**：从整理 Agent 输出的正文中提取要点，整理成结构化个人笔记。判断哪些内容值得保留，给出保留理由。

**输入**：Agent 1 输出的整理清单中有正文的页面内容。

**输出**：
- 3~5 条核心要点（每条标注正文出处位置）
- 一句"为什么值得保留"
- 修订建议：值得深读 / 参考写法 / 补充数据 / 可忽略

**铁律**：
- 只依据正文。正文里没有的数字、结论，一律不写
- 每个具体数字、结论都标出它在正文里的位置（第几段、哪句话附近）
- 标题常常比正文激进，不要照标题推断内容
- 你产出的是笔记，不是判决。值不值得留你给建议，最后由用户定
- 原文文件只读，不可修改

**提示词**（复制到新 Agent/对话框）：

```
你是笔记提炼员。我会给你一段网页正文。

对这段内容：
1. 提取 3 到 5 条核心要点，只能来自正文。
2. 给一句"为什么值得保留"。
3. 给出修订建议：值得深读 / 参考写法 / 补充数据 / 可忽略。

每个具体数字、结论，都标出它在正文里的位置（第几段、哪句话附近），
方便后面核对。

铁律：
- 只依据正文。正文里没有的数字、结论，一律不写
- 标题常常比正文激进，不要照标题推断内容
- 你产出的是笔记，不是判决。保留建议由你提，最终由用户定
```

**自动化调用**：

```
读入整理 Agent 清单中有正文的页面，每页提取 3 到 5 条核心要点 + 一句保留理由 + 修订建议。
每个数字和结论标出正文出处位置。
只依据正文，不要照标题推断。
```

---

## Agent 3 · 核查

**角色**：笔记核查员。

**负责**：拿 Agent 2 提炼的每条要点、每个数字、每个结论，回到 Agent 1 的原文逐条比对。专抓 AI 编造的内容。

**这是整套里最容易被跳过、也最关键的一道。** AI 提炼东西的时候会一脸自信地编，它不是故意骗你，是把听起来对的当成对的写出来。没有这一道，一条假数字就会被你当真存进知识库，你还不知道存错在哪。

**铁律：核查必须独立，不能自己查自己。** 写笔记的 Agent（提炼）不能来核查自己。核查必须是一个全新的 Agent，一份干净的上下文。

**输入**：Agent 1 的整理清单（含原文路径，Agent 通过路径读取原文，只读不改） + Agent 2 的笔记。

**输出**：

| 笔记条目 | 状态 | 正文依据 |
|----------|------|----------|
| 每条要点 | 已核实 / 可疑 | 原文出处位置或冲突说明 |

**提示词**（复制到新 Agent/对话框）：

```
你是笔记核查员。我会给你两份材料：
1. 网页原文（来自整理清单中的原文路径，只读）
2. 提炼笔记（Agent 2 写的）

把笔记里每一条要点、每个数字、每个结论，回到原文逐条比对：
- 对得上的，标"已核实"，写出在正文里的位置
- 对不上、或原文里根本找不到的，标"可疑 / 原文无依据"
  说清冲突在哪（比如笔记写"翻倍"，正文只说"稳步增长约 8%"）

铁律：
- 只依据我给的原文判断，不要用你自己的知识帮笔记圆场
- 你的活是比对，不是回忆。原文没写的，就是没写
- 你是全新的 Agent，和提炼笔记的不是同一个，不要替它辩护
- 原文文件只读，不可修改

最后给我一张表：笔记条目 | 状态（已核实 / 可疑） | 正文依据
```

**自动化调用**：

```
把提炼笔记里的每条事实、数字、结论，逐条对照原文比对。
与正文相悖或正文根本没有的标"可疑 / 原文无依据"。
给一张核查表（笔记条目 | 状态 | 正文依据）。
注意：你是全新的 Agent，和提炼笔记的不是同一个。
原文文件只读，不可修改。
```

---

## Agent 4 · 确认

**角色**：确认员。

**负责**：把前三道 Agent 的产出汇总成一份"待确认提案"，每条给一个建议动作。**但只出建议，不动手。**

这是整套流程里唯一一道"人来按确认键"的闸。前面三道再能干，也只是把活干完、把建议摆好；按不按那个确认键，从头到尾是你。

**输入**：Agent 1 整理清单 + Agent 2 笔记 + Agent 3 核查表。

**输出**：

| 条目 | 建议动作 | 理由 | 证据 |
|------|----------|------|------|
| 每条要点 | 保留 / 修正 / 删除 | 为什么 | 核查结果引用 |

- 核查标"可疑"的条目单独列出，等你逐条核对
- 所有删除 / 发布 / 存档动作停在"待确认"，不自动执行

**提示词**（复制到新 Agent/对话框）：

```
你是确认员。我会给你三份材料：
1. 整理清单（Agent 1 输出的）
2. 提炼笔记（Agent 2 写的）
3. 核查表（Agent 3 核的，其中有些条目被标了"可疑"）

你的任务不是再核一遍原文（核查已经做完了），
而是把这些汇总成一份"待确认提案"。

对每条笔记要点输出一行：
条目 | 建议动作（保留 / 修正 / 删除） | 理由 | 证据位置。

核查标"可疑"的条目，单独列出，等我逐条核对。

铁律：
- 所有删除 / 发布 / 存档都是提案，不自动执行，等我逐条点确认后才落地
- 拿不准时宁可多留一条让我决定，也不要替我删。误删的资料找不回来
- 你不是再核一遍，你是汇总建议
```

**自动化调用**：

```
读入前三道 Agent 的产出（整理清单 + 提炼笔记 + 核查表），
汇总成一份待确认提案，每条给建议动作（保留 / 修正 / 删除）。
核查标"可疑"的单独列出等我核对。
全部停在"待确认"，不要执行任何删除或发布。
```

---

## 确认边界：什么 AI 能自动做，什么必须等你

这套分工最重要的不是自动化，是**可控的自动化**。AI 出提案，你按确认键。判断标准很简单：**这个动作如果错了，撤得回来吗？** 撤不回来、或者很麻烦，就停在待确认。

### 必须停在"待确认"的动作

| 动作 | 为什么危险 | 应该怎样 |
|------|-----------|---------|
| 删除任何资料、笔记、文件 | 删了找不回来，最不可逆 | 只标"建议删除"，你点头才删 |
| 归档 / 收起 / 移走 | 等于半个删除，眼不见就再也不翻 | 只标"建议归档"，你确认 |
| 发布（发文章、发社交媒体） | 发出去全网看到，撤回也留痕 | 出草稿，你过目才发 |
| 改长期记忆 / 知识库 / 笔记 | 一条假信息进了长期库，以后一直拿它当真 | 标出来，你核对才写进去 |
| 改文件名 / 大批移动文件 | 一动一大片，引用全断 | 列清单，你确认才动 |

### 可以让 AI 直接干的

- 读、归类、提要点、列清单、出提案（"摆出来给你看"，没动你任何东西）
- 在临时草稿、临时对话里改来改去
- 给建议、做对比、标可疑

**一句话：AI 可以无限地"想"和"建议"，但凡要"落地动手"，停在你这道闸前。**

### 通用保险条款

任何一个 Agent 的提示词末尾都可以追加这句，当保险：

```
重要：你只负责产出和建议，不替我执行任何删除、归档、发布、发送、
或写入长期记录的动作。所有这类动作都列成"待确认"，等我点头。
```

---

## 跨工具使用

### 方式一：作为 Skill 自动加载（推荐）

将四个 Agent 的提示词各存为独立 Skill，放在一个共享 Skill 目录，再让各工具通过 Junction 指向它。改一处，所有工具一起生效。

```
源目录（唯一真相源）：
<你的 Skills 目录>/web-content-capture/整理/SKILL.md
<你的 Skills 目录>/web-content-capture/提炼/SKILL.md
<你的 Skills 目录>/web-content-capture/核查/SKILL.md
<你的 Skills 目录>/web-content-capture/确认/SKILL.md

各工具通过 Junction 指向源目录即可。
```

使用时直接说：

> "帮我按照获取网页内容四步流程，处理这批已存好的网页内容：..."

AI 工具会自动依次调用四个 Agent。

### 方式二：只用对话框（零门槛）

开四个对话框，每个粘贴一个 Agent 的提示词。第一道输出 → 第二道输入 → 第三道输入 → 第四道输入。核查那道必须另开新对话，别在提炼那个对话里接着问。

### 方式三：文件流水线（Claude Code / Codex / WorkBuddy）

把四个 Agent 的提示词做成四个 subagent，按文件流水线跑：读取用户提供的原文文件 → 逐道写出 `runs/<本次子目录>/` 下的 `01-整理清单.md` → `02-笔记.md` → `03-核查表.md` → `04-待确认提案.md`。

> **每次运行一个子目录**：子目录建在 `runs/` 下，命名 `YYYY-MM-DD-主题`（主题由用户给出，或 AI 从首批网页标题提取）。四个输出文件并列放子目录根。原文不复制、不移动，仅在整理清单中记录来源路径，核查时直接引用原文路径比对。这样多次运行互不覆盖，原文也不会被误改。

---

## 输入输出格式

### 输入（用户提供）

用户提供网页内容，格式不限，只需包含标题、网址、正文（正文为空表示存不到）。正文可以是行内文本，也可以是文件路径（调度器会读入文件内容传给 Agent，原文只读不改）。

例如：

```
标题：某数据库年度报告
网址：https://example.com/article
正文：这是已存好的完整正文...
```

或者直接给文件路径：

```
E:\notes\webpages\db-report-2026.md
E:\notes\webpages\context-engineering.md
```

### 各 Agent 输出

> 所有输出文件都写在 `runs/<本次子目录>/` 下（子目录命名见上文方式三）。下表文件名均为相对该子目录的路径。

| Agent | 输出文件 | 内容 |
|-------|----------|------|
| 整理 | `01-整理清单.md` | 标题、域名、字符数、有无正文、原文来源、初步分类 |
| 提炼 | `02-笔记.md` | 3-5 条要点 + 保留理由 + 修订建议 |
| 核查 | `03-核查表.md` | 核查状态表（已核实/可疑） |
| 确认 | `04-待确认提案.md` | 建议动作汇总（全部待确认） |

### 最终输出（给用户）

确认 Agent 在 `runs/<本次子目录>/` 下输出一份 `04-待确认提案.md`，你逐条审核后，确认的条目由 Agent 写入 Obsidian 笔记。

### 完整示例（走一遍看看）

假设用户存了这样一批网页内容：

```
标题：某数据库年度报告 · 采用率再创新高
网址：example.org/db-report-2026
正文：（4,200 字正文，其中提到"采用率稳步增长约 8%"）
---
标题：一个关于上下文工程的 YouTube 演讲
网址：youtube.com/watch?v=xxxx
正文：无正文
```

**Agent 1 整理 → `01-整理清单.md`**

| 标题 | 域名 | 正文字符数 | 有正文 | 原文来源 | 初步分类 |
|------|------|-----------|--------|---------|---------|
| 某数据库年度报告 | example.org | 4200 | 有 | 行内文本 | 学习 |
| 上下文工程演讲 | youtube.com | 0 | 无 | — | 无法分类（无正文） |

**Agent 2 提炼 → `02-笔记.md`**

```
## 某数据库年度报告 · 采用率再创新高
- 要点 1：采用率稳步增长，约 8%（正文第 3 段）
- 要点 2：……（正文第 X 段）
为什么值得留：唯一一份带真实行业数据的报告。
修订分类：学习（维持）。

## 上下文工程演讲
正文缺失，未提炼。
```

**Agent 3 核查 → `03-核查表.md`**

| 笔记条目 | 状态 | 正文依据 |
|---------|------|---------|
| 采用率稳步增长约 8% | 已核实 | 正文第 3 段原话 |
| 采用率同比翻倍 | 可疑 / 原文无依据 | 原文只说稳步增长约 8%，没有翻倍 |

（注意：如果提炼 Agent 写了"翻倍"这种原文没有的内容，核查 Agent 会揪出来。这就是为什么核查必须独立——它不知道提炼是怎么想的，只看原文和笔记逐条比对。）

**Agent 4 确认 → `04-待确认提案.md`**

| 网页 | 建议动作 | 理由 | 原判 → 新判 |
|------|---------|------|------------|
| 某数据库年度报告 | 深读 | 唯一带真实数据的报告，那条"翻倍"需你核对 | 可归档 → 深读 |
| 上下文工程演讲 | 保留 | 无正文，没法判断，先留着 | 无法分类 → 待你看 |

**全表停在"待确认"，什么都不会被删、被归档、被动。**

### 文件流水线（给 Claude Code / Codex / WorkBuddy）

如果想用文件流水线自动串起来，输入可以用 JSON 格式，引用原文文件路径（不复制）：

```json
[
  {
    "id": "p01",
    "title": "某数据库年度报告",
    "domain": "example.org",
    "url": "example.org/db-report-2026",
    "source_path": "E:/notes/webpages/db-report-2026.md"
  },
  {
    "id": "p02",
    "title": "上下文工程演讲",
    "domain": "youtube.com",
    "url": "youtube.com/watch?v=xxxx",
    "source_path": null
  }
]
```

`source_path` 为 `null` 就是没正文。原文文件只读，不可修改。

---

## 一句话纪律

**任何删除、归档、发布、改长期记忆的动作，都停在"待确认"，你点头才执行。AI 分工干活，按确认键的人是你。**

---

> 设计参考：[Agent OS 的第一课：会分工，才会用 Agent](https://www.axtonliu.ai/newsletters/ai-2/posts/agent-os-workstation-division)（分工隔离、独立核查、确认闸门三层设计）
> 工具依赖：BrowserSkill（浏览器操作）见 [[BrowserSkill Installation Guide|BrowserSkill 安装指南]]，命令参考见 [[BrowserSkill Command Reference|BrowserSkill 命令参考]]