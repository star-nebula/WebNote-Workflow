# 确认（调度器自执行）

> 产出：`runs/<run>/04-待确认提案.md`

> 本文件是操作卡，不是 agent。由**调度器（主会话）**执行，不创建独立上下文。

## 职责

读 `01/02/03` 三份产出，汇总成待确认提案。**不读原文、不重新核查、不做新判断**——核查已经做完了，你只是汇总。

## 汇总规则

对每条笔记要点给一个建议动作：

| 核查状态 | 建议动作 |
|---|---|
| 已核实（直接引用） | **保留** |
| 已核实（转述源） | **保留（待回看）** —— 建议用户自己核对原始音画后再存入长期库 |
| 可疑 / 原文无依据 | **修正或删除** —— 给出具体理由 |

**所有转述源条目都要单独列出**，不能混在"保留"里一句带过。它们是这份提案里最需要用户注意的部分。

## 铁律

- 不重新核查原文（核查已经做完了）
- 拿不准时宁可多留一条让用户决定，也不替用户删
- 所有删除、归档、发布、写入长期记录的动作都是**提案**，不自动执行

## 确认边界

判断标准：**这个动作如果错了，撤得回来吗？**

### 必须停在「待确认」

| 动作 | 为什么危险 |
|---|---|
| 删除任何资料、笔记、文件 | 删了找不回来 |
| 归档 / 收起 / 移走 | 眼不见就再也不翻 |
| 发布（发文章、发社交媒体） | 发出去全网看到 |
| 改长期记忆 / 知识库 / 笔记 | 一条假信息进了长期库 |
| 改文件名 / 大批移动文件 | 一动一大片，引用全断 |

### 调度器可以直接做的

- 媒体格式分类、调用提取工具
- 读、归类、提要点、列清单、出提案
- 在临时草稿里改来改去
- 给建议、做对比、标可疑

**一句话：AI 可以无限地"想"和"建议"，但凡要"落地动手"，停在用户这道闸前。**

## 输出 → `runs/<run>/04-待确认提案.md`

```markdown
# 04-待确认提案

> 生成时间：<日期>
> 执行者：调度器（主会话）
> 状态：**全部待确认，未执行任何落地动作**

## 汇总

| 状态 | 条数 |
|---|---|
| 已核实（直接引用） | X |
| 已核实（转述源） | Y |
| 可疑 | Z |

## 一、建议保留

| 条目 | 建议动作 | 依据 |
|---|---|---|
| p01-1 | 保留 | 已核实（直接引用），第 3 段 |

## 二、建议保留但需你回看（转述源）

| 条目 | 为什么需要回看 | 建议动作 |
|---|---|---|
| p06-1 | 数字来自 OCR，原文含"收款/点费"等误识别 | 核对原始画面后再存入 |

## 三、建议修正或删除

| 条目 | 问题 | 建议动作 |
|---|---|---|
| p03-2 | 原文未提及缓存层，因果为笔记添加 | 删除或改写 |

## 待你确认的落地动作

- [ ] <具体动作 1>
- [ ] <具体动作 2>

（以上均**未执行**，等你逐条点头）
```
